{
  "name": "aiinvoice",
  "private": true,
  "scripts": {
    "build": "cd Frontend/Invoicy && npm run build",
    "install:all": "npm install --prefix Backend && npm install --prefix Frontend/Invoicy --include=dev"
  }
}
