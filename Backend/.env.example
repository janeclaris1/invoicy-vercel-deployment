# Server Configuration
PORT=8000
NODE_ENV=development

# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Platform admin: email(s) that can see all subscribed clients (comma-separated for multiple)
PLATFORM_ADMIN_EMAIL=your-email@example.com

# CORS: comma-separated allowed origins in production (e.g. your Vercel frontend URL)
ALLOWED_ORIGINS=https://yourdomain.com,https://invoicy-vercel-deployment.vercel.app
# When frontend and backend are on different domains (e.g. Vercel + Render), set to true so auth cookie uses SameSite=None
CROSS_ORIGIN_AUTH=true

# AI Configuration (Google Gemini)
GEMINI_API_KEY=your-gemini-api-key

# GRA API (Ghana Revenue Authority) – credentials can be set per user in Settings → Company
# Optional: override GRA API base URL (default: https://vsdcstaging.vat-gh.com/vsdc/api/v1)
# GRA_BASE_URL=https://vsdcstaging.vat-gh.com/vsdc/api/v1

# Paystack (subscriptions) – get keys from https://dashboard.paystack.com → Settings → API Keys
# Use SECRET key (sk_live_... or sk_test_...), NOT the public key. Wrong key = "Invalid key" and subscription never created.
PAYSTACK_SECRET_KEY=sk_live_xxxxxxxxxxxx
# Where to send user after payment (callback_url). Local: http://localhost:5173  Production: https://your-app.vercel.app
FRONTEND_URL=http://localhost:5173

# Integrations: encrypt API keys and OAuth tokens at rest (min 32 chars for AES-256). Optional in dev.
# INTEGRATION_ENCRYPTION_KEY=your-32-char-or-longer-secret-key-here

# Logging
LOG_LEVEL=info
