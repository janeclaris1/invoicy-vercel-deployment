# API Configuration
# Local dev: use localhost. Production (e.g. Vercel): use your backend's PUBLIC URL (https://...).
# Never use localhost in Vercel env vars — the deployed app runs in users' browsers; localhost would point to their machine.
VITE_API_URL=http://localhost:8000

# GRA API Configuration (Ghana Revenue Authority)
VITE_GRA_COMPANY_REFERENCE=your-company-reference
VITE_GRA_SECURITY_KEY=your-security-key

# Environment
VITE_NODE_ENV=development
